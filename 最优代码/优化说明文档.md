# Pangu-Weather 轻量化与推理优化 技术说明文档

先导杯 2026 AI4S 赛题二 · 基于国产海光 DCU 的气象大模型轻量化与推理优化

本文档对应提交内容，依次说明：① 模型轻量化方法 ② 设计思路 ③ 性能优化与实现
④ 实验结果与分析（官方 15 指标口径）⑤ 程序代码模块说明 ⑥ 编译说明 ⑦ 运行使用说明。

------------------------------------------------------------------------

## 一、模型轻量化方法

整体思路：用一个轻量学生网络 **MaxViT-3D（5.19M 参数）** 通过**知识蒸馏 + 真值双监督**逼近
官方 Pangu 教师（64.22M 参数），再叠加**推理期显存优化**与 **FP16 权重存储**，
在不改动计时代码、不引入外部权重/数据的前提下，同时优化 U（模型大小 + 推理显存）、V（推理时长）、W（精度）。

### 1.1 知识蒸馏（轻量化主路径）

- **学生架构**：MaxViT-3D，`embed_dim=96`，`depths=(2,4,2)`，`num_heads=(6,12,6)`，`patch_size=(2,16,16)`，
  全局支路用 **grid 等距稀疏注意力**，`mlp_ratio=2.0`。U 形多尺度骨干（编码全分辨率 → 下采样 1/4 token 做主干计算 → 上采样 → 解码 + skip）。
- **注意力设计**：局部连续窗口（抓细节）与 grid 全局稀疏支路交替堆叠；采用 **QK-Norm**（q,k 先 L2 归一化再点积，余弦注意力 + 每头可学习温度，SwinV2 风格），训练更稳、可更深更窄；
  归一化用 **RMSNorm**，FFN 用 **SwiGLU 门控**，配 **LayerScale + DropPath**。
- **I/O 与教师严格一致**：输入 `[B,72,721,1440]`，输出 `surface[B,4,721,1440] + upper[B,5,13,721,1440]`；
  stem（PatchEmbed2D/3D）与 head（PatchRecovery2D/3D）**复用 OneScience 官方模块**，保证形状与教师可蒸馏、可用官方 result.py 评测。
- **两阶段真实训练**：第一阶段由 `distill_cache.py` 在实验室 Mitai 4090 上做纯官方教师蒸馏；
  第二阶段由 `distill_truth.py` 在实验室 abc66 5090 上续训，仅对 2m 温度和 13 个气压层温度使用
  `0.5 × 教师损失 + 0.5 × 官方 ERA5 真值损失`，其余 55 通道保持纯教师监督。
- **最终主干不使用 DTP**：`dtp=0`、`phys=0`、`residual=0`。第一阶段 83 个二维骨干权重走
  Muon（lr 0.02、momentum 0.99），其余参数走 AdamW；第二阶段 lr 为 6e-5、BF16。

### 1.2 模型结构优化（最终权重实际启用）

- **轻量 MaxViT-3D 学生**：5.19M 参数，保持 69 个气象输出通道与官方 I/O 语义不变。
- **三维非重叠 Patch**：`patch_size=(2,16,16)`，同时压缩气压层、纬度和经度 token 数。
- **U 形多尺度骨干**：编码、下采样、中层、上采样、解码及 skip 连接，在低分辨率主干计算与高分辨率细节之间折中。
- **宽深联合压缩**：`embed_dim=96`、`depths=(2,4,2)`、`num_heads=(6,12,6)`；每个 depth 单元由局部与全局两层组成。
- **局部 block + grid 全局注意力**：局部连续窗口捕获邻域结构，grid 等距稀疏分组传递远距离信息。
- **QK-Norm 与逐头温度**：q/k 做 L2 归一化，配每头可学习 logit scale，稳定窄模型注意力。
- **Pre-RMSNorm、SwiGLU、LayerScale、DropPath**：改善深层残差训练稳定性及表达效率；`mlp_ratio=2.0`。
- **官方 Stem/Head 兼容**：复用 OneScience 的 PanguEmbedding/PatchRecovery，输入输出形状与变量顺序不变。
- **直接预测完整未来场**：`residual=False`，不使用“输入 + 预测增量”的残差输出。
- **FP16 部署权重**：仅改变存储精度；最终提交不使用 INT8、权重重排或外部查表文件。

### 1.3 训练策略优化（最终权重实际启用）

- **两阶段课程式训练**：先做 69 通道纯教师蒸馏，再从第一阶段权重严格续训并引入有限真值校准。
- **冻结教师与一次性 FP16 目标缓存**：教师全程 `eval` 且不计算梯度，训练前缓存目标，避免重复教师前向。
- **第一阶段纯教师蒸馏**：1980–1996 年候选数据训练、1997 年验证，80 epoch，`max_cache=300`。
- **14 个温度通道真值校准**：2m 温度与 13 个气压层温度采用 `0.5×教师 + 0.5×官方 ERA5 真值`；其余 55 通道保持纯教师监督。
- **变量/层级权重 L1 与纬度面积权重**：沿用官方变量权重，并乘归一化 `cos(latitude)`，避免极区网格过采样。
- **Muon + AdamW 分组优化**：二维矩阵参数使用 Muon（`lr=0.02, momentum=0.99`），其余参数用 AdamW；配 warmup + cosine。
- **BF16、EMA 与梯度裁剪**：BF16 autocast，EMA `decay=0.999`，`clip_grad_norm_=1.0`。
- **经度周期 roll 增强**：对输入、教师目标与真值同步做经度循环平移，符合经度周期边界。
- **严格连续 6h pair 和固定切分**：只接受文件名时间差严格为 6h 的样本对；固定随机种子后切分训练/验证。
- **严格续训和双口径验证**：第一阶段权重 `strict=True` 加载；同时监控对教师 `val_match` 与对 t+6h ERA5 的温度 RMSE。

最终配置明确关闭且不计入有效优化的实验分支：残差预测、DTP 动态通道权重、物理特征头、频谱损失、
全 15 指标真值监督、额外风/湿真值监督、区域重加权、冻结 backbone。正式启动脚本会拒绝误开启这些分支。

### 1.4 推理期显存优化（提升 U 的显存项）

DCU 上学生跨样本峰值显存从 ~923MB 压到 **548MB**，三项措施数学等价、不改 W/RMSE：

1. **SDPA off（`set_sdpa(False)`）**：本 DCU 的 SDPA 内存高效注意力回退会分配巨大工作集（实测注意力 ~579MB）；
   窗口内 token 很小（144），naive 注意力反而**省约 199MB 且更快**。
2. **mem_opt 空间分块 tiling（推理默认开，`maxvit3d_student.forward`）**：
   - stem/head 的高分辨率卷积沿纬度分块（`stride=kernel` 非重叠 → **bit-exact**，输出与不分块完全一致）；
   - stem 之后立即 `untyped_storage().resize_(0)` 释放输入张量存储（贯穿全程的最大常驻项）；
   - 编/解码器逐 block 释放中间张量。
3. **fp16 输入构造（计时区外，数学等价）**：直接以 fp16 构造输入，避免 299MB 的 fp32 输入常驻；
   autocast 本就把 conv 输入转 fp16，提前喂 fp16 与 autocast 内部转换 bit-exact（实测 stem maxdiff=0）。

### 1.5 权重存储（提升 U 的模型大小项）

- **提交权重以 fp16 半精度存储**：`student.pth` 仅 **10.46MB**（去除优化器/冗余状态，仅留 `model_state_dict + cfg`）。
  推理本就走 autocast fp16，fp16 权重加载进模型后与 fp32 权重 autocast 推理**数值等价**
  （归一化空间 maxdiff≈0.008、meandiff≈3e-4，官方 15 指标 W 与 fp32 权重完全一致）。
- **本提交不含 INT8 量化、反量化表或权重重排**。最终推理只加载随包提交的 FP16 学生权重，路径与实现均可审计。

------------------------------------------------------------------------

## 二、设计思路

- **U/V/W 联合最优**：单纯压参数会掉精度（W）、单纯保精度则模型大/慢（U/V）。本方案以“**够小够快的学生 + 蒸馏对齐教师**”为主线，
  让学生在 5.19M（教师的 8.1%）规模下逼近教师，从而 U（大小+显存）、V（时长）大幅领先，W 通过蒸馏 + 真值双监督守住。
- **为什么 MaxViT-3D / grid 注意力**：气象场是规则网格、长程依赖强。grid 等距稀疏全局注意力以 O(N·M) 复杂度覆盖全局，
  对 DCU 稠密 GEMM 友好；U 形下采样把主干算力放在 1/4 token 上，兼顾感受野与算力/显存。
- **为什么蒸馏 + 真值双监督**：SCNet 环境有连续 6h 真值对，既可对齐教师表达（match），又可直接对真实未来场学习（技能），
  双路监督比单蒸馏更稳、技能更高。
- **为什么显存优化要“数学等价”**：W 评测对数值敏感，所有显存措施（分块/释放/fp16 输入）均保证 bit-exact 或差异远小于评测分辨率，
  确保“省显存不掉分”。
- **为什么计时加 synchronize**：DCU 算子异步，不同步会低估 GPU 真实耗时；按官方更新在 `model(invar)` 后、`end_time` 前
  新增 `torch.cuda.synchronize()`，使上报时长为真实同步耗时，与评测一致。

------------------------------------------------------------------------

## 三、性能优化与实现

| 优化项 | 实现位置 | 效果 |
|---|---|---|
| 知识蒸馏轻量化 | distill_cache.py / distill_truth.py | 参数 64.22M → 5.19M（8.1%），时长 979.9ms → 65.7ms |
| SDPA off | maxvit3d_student.set_sdpa(False)（inference.py 调用） | 注意力工作集 −199MB，更快 |
| mem_opt 空间分块 + 及时释放 | maxvit3d_student.forward（eval 默认开） | 峰值 923MB → 548MB（bit-exact） |
| fp16 输入构造 | inference.py（计时区外） | 输入常驻 −299MB（与 autocast 等价） |
| autocast fp16 推理 | inference.py 计时区内 | DCU 半精度算力，提速 |
| fp16 权重存储 | student.pth（10.46MB） | 模型大小 17.47MB → 10.46MB（数值等价） |

实现要点（与代码一致）：
- 推理入口 `inference.py` 加载 `data/checkpoints/student.pth`，从权重内嵌 `cfg` 重建学生结构（无需外部结构文件）；
  `set_sdpa(False)` + autocast fp16；计时区严格按官方写法 + 新增 `torch.cuda.synchronize()`。
- `maxvit3d_student.forward` 在 eval 模式自动走 mem_opt 路径；需关闭可设 `model.mem_opt=False`（结果不变，仅显存更高）。

------------------------------------------------------------------------

## 四、实验结果与分析（官方 15 指标口径）

测试环境：海光 DCU，ERA5 测试集逐对 6h 预报；时长/显存均为**同步口径**（`model` 前后 `torch.cuda.synchronize()` 才计时，与提交 inference.py 一致）。
W 严格按赛题第 7 条**只评 15 个气象指标**：mslp / 10m_u / 10m_v / 2m_t / geopotential_{850,700,500} /
specific_humidity_{850,700,500} / temperature_{850,700,500} / u_500 / v_500。

### 4.1 总体对比

| 项目 | 教师 Pangu | 学生 MaxViT-3D（本提交） |
|---|---|---|
| 参数量 | 64.22M | 5.19M（8.1%） |
| 权重文件 | 1868MB（model_bak.pth） | 10.46MB（fp16，student.pth） |
| 推理峰值显存 | 12469MB | 548MB |
| 推理时长（同步口径） | 979.9ms | 65.7ms（**14.9× 加速**） |
| 15 指标 RMSE 均 | 17.601 | 20.107 |
| 15 指标 ACC 均 | 0.9920 | 0.9897 |

### 4.2 得分（官方口径）

- **U = 39.01 / 40**
  - 模型大小项 = (1 − 10.46/1868) × 20 = **19.89**
  - 推理显存项 = (1 − 548/12469) × 20 = **19.12**
- **V = 19.40 / 20**　（S = 979.9/65.7 = 14.90×，V = (1 − 1/S^1.3) × 20）
- **W = 35.63 / 40**（官方 15 指标口径；对照 69 通道口径 W = 36.24）
- **总分 = U + V + W = 94.04**（官方 15 指标口径；69 通道口径为 94.65）

> 口径说明：15 指标里包含 mslp / 10m_u / 10m_v / 2m_temperature 四个**近地面**变量，
> 这正是学生相对教师退化最多的项（约 1.17–1.19×，合分 1.69–1.73 / 满分 2），
> 拉低了 15 指标均值；而 69 通道里大量高层 z/t/q/u/v 拟合更好（合分 1.8+）会抬高均值。
> 因此 **官方 15 指标口径 W=35.63、总分=94.04 为真实提交口径**，略低于 69 通道口径，文档以官方口径为准。

### 4.3 15 指标逐项（RMSE 项 + ACC 项，满分各 1，合 2）

| 指标 | 学生RMSE/教师RMSE | 合分(满2) |
|---|---|---|
| geopotential_500 | 1.07× | 1.866（最好） |
| specific_humidity_850 | 1.07× | 1.861 |
| temperature_500 | 1.08× | 1.852 |
| temperature_700 | 1.08× | 1.850 |
| specific_humidity_700 | 1.10× | 1.824 |
| geopotential_700 | 1.10× | 1.821 |
| temperature_850 | 1.12× | 1.798 |
| specific_humidity_500 | 1.11× | 1.795 |
| v_component_of_wind_500 | 1.13× | 1.776 |
| u_component_of_wind_500 | 1.15× | 1.747 |
| 2m_temperature | 1.17× | 1.734 |
| geopotential_850 | 1.19× | 1.710 |
| mean_sea_level_pressure | 1.19× | 1.704 |
| 10m_u_component_of_wind | 1.19× | 1.697 |
| 10m_v_component_of_wind | 1.19× | 1.687（最差） |

分析：高层场（500/700 hPa 的 z/t/q/u/v）蒸馏对齐最好（合分 1.8+）；近地面风/气压/温度受地形与边界层影响、退化相对明显，
是后续提分的主攻方向（如近地面通道加权、地形物理先验）。当前权重在官方 15 指标口径下已达 **94.04 总分**。

------------------------------------------------------------------------

## 五、程序代码模块说明

- **inference.py**：黑盒测试推理入口。读 `conf/config.yaml`、官方归一化统计与静态场，构建数据管线；
  加载 `data/checkpoints/student.pth`（fp16，从内嵌 cfg 重建结构）；`set_sdpa(False)` + fp16 输入 + autocast fp16；
  计时区严格按官方（`model(invar)` 后、`end_time` 前 `torch.cuda.synchronize()`，标注 AI4S 不可改）；
  输出 `result/output/*.npy` 与 `result/time_record.json`。
- **maxvit3d_student.py**：学生模型 MaxViT-3D 定义。含 grid/spectral/fpa 三种全局支路（默认 grid）、QK-Norm、RMSNorm、SwiGLU、
  `set_sdpa()` 开关、eval 默认开的 mem_opt 空间分块 forward（bit-exact 省显存）。stem/head 复用 OneScience 官方 PanguEmbedding/Recovery。
  **模型直接输出完整预测场（与输入同通道数），不含任何"输出 = 输入 + 增量"的残差预测路径。**
- **distill_cache.py**：实验室 4090 第一阶段真实脚本，纯官方教师蒸馏、Muon + AdamW、EMA。
- **distill_truth.py**：实验室 5090 第二阶段真实脚本，构造连续 6h pair，并对 14 个温度通道加入官方 ERA5 真值监督。
- **muon.py**：第一阶段使用的 Muon 优化器实现。
- **train.py**：官方训练脚本（保留以便教师侧/基线对照复现）。
- **result.py**：官方评估与可视化脚本。
- **training/run_stage1_4090.sh / run_stage2_5090.sh**：两阶段完整环境变量、资产检查和启动参数。
- **build_hip.py**：显式编译并加载随包 HIP 源码。
- **verify_submission.py**：检查必要文件、源码语法、学生权重及预编译文件边界。
- **conf/config.yaml**：数据管线配置（通道列表、归一化/静态场路径、img_size、逐通道权重）；test_ratio 为占位年份。
- **data/checkpoints/student.pth**：学生轻量权重（fp16，含 `model_state_dict + cfg`）。

------------------------------------------------------------------------

## 六、编译说明

### 6.1 HIP 源码现场编译

提交包**不包含任何预编译 `.so` / `.pyc` / `.o`**。`src/attn_lib.hip` 与
`src/gemm_lib_mmac.hip` 是完整可审计源码；`maxvit3d_cpp.py` 在正式计时前使用赛方镜像内
`hipcc -O3 --offload-arch=gfx936 -fPIC -shared` 一次性编译、加载和预热。编译失败时回退到
PyTorch 路径。编译产物只写评测机临时目录，不随包提交。

### 6.2 显存优化手段

**（1）BLAS 后端选用 hipBLASLt —— 省约 155 MB 显存，且更快**

PyTorch 在 ROCm/DCU 上有两个 GEMM 后端：rocBLAS（内部依赖 Tensile 内核库）与 hipBLASLt。
本卡实测（4096×4096 fp16 GEMM，测整卡显存增量）：

- rocBLAS：BLAS 库常驻显存 **+212.3 MB**，首次 matmul 3.44 s
- **hipBLASLt：BLAS 库常驻显存 +108.9 MB，首次 matmul 0.85 s**

端到端实测（本模型完整前向，整卡峰值）：rocBLAS **1107.7 MB** → hipBLASLt **953.1 MB**，
**省 154.6 MB**，且前向更快。故在 `inference.py` 中于 `import torch` **之前**设置：

```python
os.environ.setdefault("TORCH_BLAS_PREFER_HIPBLASLT", "1")   # 必须在 import torch 前设置才生效
```

**（2）mem_opt 空间分块前向（bit-exact）**

推理期前向对高分辨率的 stem / recovery 卷积沿纬度分块（stride = kernel、非重叠，
故与整块计算 **bit-exact**）；stem 之后立即释放输入张量存储；encoder/decoder 逐 block 释放中间张量。
分块数由 `MEM_TILE` 控制，实测扫描（6/12/24/36/48）后取 **6** 为最优（峰值最低且最快）。

**（3）注意力分块开关（默认关闭，保留备用）**

`Attention3D` 支持按窗口维分块计算注意力（`ATTN_CHUNKS`），各窗口 softmax 相互独立，
与全量计算数学等价。实测：本模型在 mem_opt 前向下已逐 block 处理，注意力分数矩阵**并非显存峰值来源**，
分块不降峰值反而增加 kernel launch 开销（慢约 14 ms），故默认 `ATTN_CHUNKS=1`（关闭）。

**（4）分配器与碎片控制**

- `PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True`（可伸缩段，抑制碎片，须在 import torch 前设置）
- 每个样本推理后 `torch.cuda.empty_cache()`，避免碎片累积抬高后续样本峰值
- 输入与权重全程 fp16 构造，避免 fp32 副本常驻

**（5）注意力后端选择**

PyTorch 原生 SDPA 的 `flash` / `mem_efficient` backend 在本 DCU 上实测**不可用**
（报 `No available kernel`，仅 math backend 可用，且 math backend 会物化 N×N 分数矩阵），
故 `set_sdpa(False)` 走显式实现，实测显存更低。

### 6.3 合规说明（对应赛题技术方案第 4、11、12 条）

- **无预编译二进制**：提交内容不含 `.so` / `.pyc` / `.o`；HIP 源码、编译命令和 Python 调用均随包提交，现场编译发生在计时区外。
- **无闭源二进制工具**：未使用任何第三方闭源库、在线服务、外部大模型接口或不可审计的自动化工具。
- 所有优化均为**等价的显存与执行路径优化**（BLAS 后端选择、空间分块、分配器配置），
  不改变模型结构、权重与计算语义，不做近似、跳算或稀疏化。
- 预测输出为**完整场**（与输入通道数一致，覆盖全部评价与非评价通道）。

### 6.4 环境依赖

OneScience 0.3.0 / Python 3.11 / PyTorch 2.5.1（DCU）/ numpy / h5py / tqdm / hipcc，
均为赛题官方环境自带，**无需安装任何额外依赖**。
可选压缩相关库（同属官方环境，本提交推理不依赖）：onnx 1.21.0 / onnxsim 0.6.3 /
onnxconverter-common 1.16.0 / tensorly 0.9.0 / torch-pruning 1.6.1。

------------------------------------------------------------------------

## 七、运行使用说明

```bash
source ../earth_env.sh                 # 加载官方环境

# 推理（黑盒测试入口）：结果 → ./result/output/，时长 → ./result/time_record.json
python inference.py

# 评估 / 可视化
python result.py

# 阶段 1：实验室 Mitai 4090，纯官方教师蒸馏
python distill_cache.py --opt muon --muon-lr 0.02 --muon-momentum 0.99 \
  --embed 96 --depths 2,4,2 --heads 6,12,6 --patch 2,16,16 \
  --global-mode grid --mlp-ratio 2.0 --train-years 1980,...,1996 \
  --val-years 1997 --epochs 80 --max-cache 300 --amp bf16 --aug 1 \
  --residual 0 --dtp 0 --spec 0 --save student_ratio2_mom99.pth

# 阶段 2：实验室 abc66 5090，14 温度通道有限真值监督
python distill_truth.py --years 1980 --epochs 35 --max-cache 300 --val-frac 0.1 \
  --truth-alpha 0.5 --full-temp 1 --phys 0 --amp bf16 --lr 6e-5 \
  --warmup 2 --dtp 0 --resume student_ratio2_mom99.pth --save student_truth_ft50.pth

```

注意事项：
- 教师权重、ERA5 数据、统计量、静态场均由官方环境提供（`../onedatasets` 与官方 checkpoints），**不在本包内**。
- 不要修改计时区代码与赛题标注「不可更改」的参数。

------------------------------------------------------------------------

## 八、合规声明（对照赛题技术方案）

1. **模型来源（第 4 条）**：学生模型权重完全由官方 pangu-weather 全量模型（教师）蒸馏；第二阶段仅
   对 14 个温度通道以 0.5 权重加入官方 ERA5 真值，其余 55 通道仍为纯教师监督。未使用任何外部气象模型、
   外部预训练权重或组委会未提供的数据。实验室 4090/5090 只承担存储和训练计算。

2. **无残差预测 / 无残差监督**：学生模型**直接一次性预测完整输出场**（与输入通道数一致，覆盖全部评价与
   非评价通道），**不存在**"输出 = 输入 + 增量"的恒等残差路径；训练时监督对象为完整预测场，
   **不做**残差监督（不学 delta、不用"残差 + 真值"）。网络内部 Transformer block 的层间残差连接
   （`x + F(x)`）属于常规结构设计，不涉及输入到输出的恒等映射。

3. **无投机解码 / 无跳算**：单次前向直接输出结果；无任何形式的投机解码、稀疏或近似注意力、
   token pruning、early-exit、层跳过或通道跳过；不对非评价气象指标做任何硬编码。

4. **无预编译二进制（第 4、11 条）**：提交包不含任何预编译 `.so` / `.pyc` / `.o`；
   自研 HIP 以完整源码提交，由 `build_hip.py` 或 `inference.py` 在评测机上、正式计时前现场编译。
   未使用任何闭源二进制工具、在线服务或外部接口。

5. **优化均为等价变换**：所有优化（hipBLASLt 后端选择、mem_opt 空间分块、分配器配置）
   均不改变模型结构、权重与计算语义，不做近似、跳算或稀疏化；空间分块为非重叠切分，
   与整块计算 bit-exact。

6. **计时与输出规范**：AI4S 计时区代码位置未改动（`torch.cuda.synchronize()` 位置符合官方模板）；
   输出为 `result/output/*.npy`（完整通道）与 `result/time_record.json`。

7. **第三方代码标注（第 12 条）**：模型 stem/head 复用 OneScience 官方 `PanguEmbedding` /
   `PanguPatchRecovery` 模块；数据管线、评测脚本沿用官方实现。除此之外的学生骨干网络、
   全部 HIP 核、训练与推理优化脚本均为本队自研。
- 如需关闭显存优化对照，设 `model.mem_opt=False`（结果不变，仅显存更高）。
